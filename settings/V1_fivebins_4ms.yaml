cross_validated_optimization : False

# Step size delta t (in seconds) with which the window is slid through the data.
embedding_step_size : 0.004

ANALYSIS_DIR : /data.nst/lucas/history_dependence/historydependence/analysis/V1/analysis_fivebins_4ms
